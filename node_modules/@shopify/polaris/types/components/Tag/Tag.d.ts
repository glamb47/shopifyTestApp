/// <reference types="react" />
export interface Props {
    /** Content to display in the tag */
    children?: string;
    /** Disables the tag  */
    disabled?: boolean;
    /** Callback when tag is removed */
    onRemove?(): void;
}
export default function Tag({ children, disabled, onRemove }: Props): JSX.Element;
