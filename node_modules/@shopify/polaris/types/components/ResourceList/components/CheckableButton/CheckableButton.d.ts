/// <reference types="react" />
export interface Props {
    accessibilityLabel?: string;
    label?: string;
    selected?: boolean | 'indeterminate';
    selectMode?: boolean;
    plain?: boolean;
    measuring?: boolean;
    disabled?: boolean;
    onToggleAll?(): void;
}
export default function CheckableButton({ accessibilityLabel, label, onToggleAll, selected, selectMode, plain, measuring, disabled, }: Props): JSX.Element;
