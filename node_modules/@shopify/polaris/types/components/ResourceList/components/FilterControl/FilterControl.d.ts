/// <reference types="react" />
import { ComplexAction } from '../../../../types';
import { AppliedFilter, Filter } from './types';
export interface Props {
    searchValue?: string;
    appliedFilters?: AppliedFilter[];
    additionalAction?: ComplexAction;
    focused?: boolean;
    filters?: Filter[];
    placeholder?: string;
    onSearchBlur?(): void;
    onSearchChange(searchValue: string, id: string): void;
    onFiltersChange?(appliedFilters: AppliedFilter[]): void;
}
export default function FilterControl({ searchValue, appliedFilters, additionalAction, focused, filters, placeholder, onSearchBlur, onSearchChange, onFiltersChange, }: Props): JSX.Element;
