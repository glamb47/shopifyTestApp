import React from 'react';
import { SortDirection, VerticalAlign } from '../../types';
export interface Props {
    content?: React.ReactNode;
    contentType?: string;
    firstColumn?: boolean;
    truncate?: boolean;
    header?: boolean;
    total?: boolean;
    sorted?: boolean;
    sortable?: boolean;
    sortDirection?: SortDirection;
    defaultSortDirection?: SortDirection;
    verticalAlign?: VerticalAlign;
    onSort?(): void;
}
export default function Cell({ content, contentType, firstColumn, truncate, header, total, sorted, sortable, sortDirection, verticalAlign, defaultSortDirection, onSort, }: Props): JSX.Element;
