/// <reference types="react" />
import { IconProps } from '../../types';
interface Props extends IconProps {
}
export default function Icon({ source, color, backdrop, accessibilityLabel, }: Props): JSX.Element;
export {};
