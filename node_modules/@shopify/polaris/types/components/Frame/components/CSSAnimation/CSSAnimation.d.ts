import React from 'react';
export declare enum AnimationType {
    Fade = "fade"
}
export interface Props {
    in: boolean;
    className: string;
    type: AnimationType;
    children?: React.ReactNode;
}
export default function Collapsible({ in: inProp, className, type, children, }: Props): JSX.Element;
