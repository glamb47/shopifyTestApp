/// <reference types="react" />
export interface Props {
    open: boolean;
    onDiscard(): void;
    onCancel(): void;
}
export default function DiscardConfirmationModal({ open, onDiscard, onCancel, }: Props): JSX.Element;
