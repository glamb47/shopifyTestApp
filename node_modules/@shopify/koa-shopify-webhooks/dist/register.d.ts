import { Topic } from './types';
export interface Options {
    topic: Topic;
    address: string;
    shop: string;
    accessToken: string;
}
export declare function registerWebhook({ address, topic, accessToken, shop, }: Options): Promise<{
    success: boolean;
    result: any;
}>;
